{
  "name": "@ferg-cod3s/codeflow",
  "version": "0.24.0",
  "description": "CLI tool to convert base-agents, commands, and skills to OpenCode format with advanced optimization and parallel processing",
  "main": "dist/cli/index.js",
  "type": "module",
  "bin": {
    "codeflow": "dist/cli/index.js"
  },
  "files": [
    "dist/**/*",
    "plugins/**/*",
    "README.md",
    "LICENSE",
    "CHANGELOG.md",
    "PLUGIN_ECOSYSTEM.md"
  ],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "scripts": {
    "build": "tsc",
    "build:cli": "tsc && chmod +x dist/cli/index.js",
    "build:plugins": "tsc --project plugins/tsconfig.json",
    "dev": "tsc --watch",
    "dev:plugins": "tsc --project plugins/tsconfig.json --watch",
    "test": "bun test",
    "test:plugins": "bun test tests/plugins.test.ts",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "typecheck": "tsc --noEmit",
    "typecheck:plugins": "tsc --project plugins/tsconfig.json --noEmit",
    "lint": "tsc --noEmit && echo 'TypeScript compilation completed successfully'",
    "lint:fix": "echo 'No auto-fix available - fix TypeScript errors manually'",
    "quality": "bun run typecheck && bun run test:coverage",
    "start": "node dist/cli/index.js",
    "prepublishOnly": "bun run quality && bun run build:cli",
    "release": "bun run quality && bun run build:cli && git add . && git commit -m \"chore: update build for release\" && git tag -a v$(npm pkg get version) -m \"Release v$(npm pkg get version)\" && echo \"✅ Release v$(npm pkg get version) ready for push\"",
    "release:dry": "bun run quality && bun run build:cli && echo \"🔍 Dry run completed - ready for release\"",
    "postversion": "git push && git push --follow-tags && npm publish",
    "preversion": "bun run quality"
  },
  "keywords": [
    "cli",
    "opencode",
    "agents",
    "conversion",
    "plugins",
    "mcp",
    "anthropic"
  ],
  "author": "CodeFlow",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ferg-cod3s/codeflow.git"
  },
  "bugs": {
    "url": "https://github.com/ferg-cod3s/codeflow/issues"
  },
  "homepage": "https://github.com/ferg-cod3s/codeflow#readme",
  "dependencies": {
    "@opencode-ai/plugin": "^1.0.80",
    "chalk": "^5.6.2",
    "commander": "^14.0.2",
    "fs-extra": "^11.3.2",
    "glob": "^13.0.0",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.2",
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
