node_modules/
bun.lockb
.DS_Store

# Platform-specific agent directories - these are generated from source agents
# and should not be committed to version control
.claude/agents/
opencode-agents/

# Keep codeflow OpenCode commands (but ignore platform-specific agents)
!.opencode/

# Keep codeflow Claude Code commands (but ignore platform-specific agents)
!.claude/
